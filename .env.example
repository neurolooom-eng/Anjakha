# Base URL of the deployed Google Apps Script Web App (see apps-script/Code.gs).
# Leave unset to run entirely on bundled mock data.
VITE_SHEETS_API_URL=

# Optional: explicit Spreadsheet ID, if the Apps Script is bound to more than one sheet.
VITE_SHEETS_SPREADSHEET_ID=
